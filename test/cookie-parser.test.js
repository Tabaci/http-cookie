
const should = require('chai').should

describe('cookie-parser', function () {
	describe('#parseWith (request)', function () {
		it('should parse all cookies from a cookie string and return a CookieManager')
		it('should work even when the cookie header is undefined')
	})
	
	describe('#parseFrom (cookieString)', function () {
		it('should parse all cookies from an IncomingMessage and return a CookieManager')
	})
})
